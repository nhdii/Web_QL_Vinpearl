@model IEnumerable<QL_Vinpearl.Models.SOCA>

@{
	ViewBag.Title = "Index";
	Layout = "~/Areas/Admin/Views/Shared/_LayoutAdmin.cshtml";
}
<main>
	<div class="container-fluid px-4">
		<div class="my-3">
			@Html.ActionLink("Thêm mới", "Create", null, new { @class = "btn btn-primary" })
		</div>
		<div class="card mb-4">
			<div class="card-header">
				<i class="fas fa-table me-1"></i>
				Bảng số ca
			</div>
			<div class="card-body">
				<table id="datatablesSimple">
					<thead>
						<tr>
							<th>Mã Ca</th>
							<th>Mã Nhân Viên</th>
							<th>Số Ca</th>
							<th>Chức Năng</th>
						</tr>
					</thead>
					<tbody>
						@foreach (var item in Model)
						{
							<tr>
								<td>@Html.DisplayFor(modelItem => item.maCa)</td>
								<th>@Html.DisplayFor(modelItem => item.maNV)</th>
								<th>@Html.DisplayFor(modelItem => item.soCa1)</th>
								<td>
									<div class="d-flex justify-content-center">
										<a href="@Url.Action("Edit", new { maCa = item.maCa, maNV = item.maNV })"><i class="fa-solid fa-lg fa-pen-to-square"></i></a>
										<div class="ms-2"></div>
										<a href="@Url.Action("Delete", new { maCa = item.maCa, maNV = item.maNV })"><i class="fa-regular fa-lg fa-trash-can"></i></a>
									</div>
								</td>
							</tr>
						}
					</tbody>
				</table>
			</div>
		</div>
	</div>
</main>
